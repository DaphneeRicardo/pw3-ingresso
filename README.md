# pw3-ingresso
Ricardo e Daphne

CREATE TABLE TBL_SESSAO(
  ID_SESSAO BIGINT PRIMARY KEY IDENTITY,
  DT_DATA DATE,
  TM_HORA TIME,
  NR_PRECO FLOAT,
  TX_SALA VARCHAR(2),
  FK_FILME BIGINT FOREIGN KEY REFERENCES TBL_FILME (ID_FILME),
);
